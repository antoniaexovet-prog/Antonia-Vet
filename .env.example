DATABASE_URL="file:./dev.db"

# Credenciales del usuario administrador (veterinaria)
ADMIN_EMAIL="admin@antoniavet.cl"
ADMIN_PASSWORD="cambia-esta-clave"

# Secreto usado para firmar la sesión del panel de administración
AUTH_SECRET="cambia-este-secreto-por-uno-largo-y-aleatorio"

# Configuración de envío de correo (informes de consulta).
# Si se dejan vacías, los correos se mostrarán en la consola del servidor (modo demo).
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM="Antonia Vet <no-reply@antoniavet.cl>"
